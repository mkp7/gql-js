const fs = require('fs')

// Parse GraphQL Schema
const RawSchema = fs.readFileSync('todo.gql', { encoding: 'utf8' })

// SERVER
// Define types
// Define fields on those types
// Define resolvers

// CLIENT-REQUEST
// Validation
// Execution
// Introspection
