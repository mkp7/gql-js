const SchemaParser = require('./schema_parser')

module.exports = { SchemaParser }

// SERVER
// Define types
// Define fields on those types
// Define resolvers

// CLIENT-REQUEST
// Validation
// Execution
// Introspection
